type: rpm-ostree
remove:
  - firefox            # Remove in favor of flatpak
  - firefox-langpacks  # Remove dependency that doesn't get removed automatically
  - lxqt-policykit     # Remove in favor of polkit-gnome
